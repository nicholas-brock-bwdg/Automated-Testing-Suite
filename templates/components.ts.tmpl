import { test, expect } from '@playwright/test';

// Auto-generated by bootstrap.py — do not edit manually.
// View: {{view_path}}  ID: {{view_id}}

test.describe('{{view_id}}', () => {
  test('components — no broken Perspective components', async ({ page }) => {
    await page.goto('{{view_url}}', { timeout: 15000 });
    await expect(page.locator('.ia_perspectiveRoot')).toBeVisible({ timeout: 15000 });

    // No component error boundaries
    await expect(page.locator('.ia_componentError')).toHaveCount(0);
    await expect(page.locator('[class*="error-boundary"]')).toHaveCount(0);

    // No components in a loading-failed state
    await expect(page.locator('[data-component-state="error"]')).toHaveCount(0);
  });
});
